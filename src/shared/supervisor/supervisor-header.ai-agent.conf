[supervisord]
; The agent variant has no Laravel /app/storage tree; keep supervisord's own
; log/pid in a path the non-root laravel user can always write. Worker output
; (claude-threads) goes to the container stdout/stderr via its own program conf.
logfile=/tmp/supervisord.log
logfile_maxbytes=0
pidfile=/tmp/supervisord.pid
